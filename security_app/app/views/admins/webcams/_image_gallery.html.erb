<% if images %>
  <ul class="clearing-thumbs small-block-grid-6" data-clearing>
    <% images.each do |image| %>
      <li><%= link_to image_tag(image.authenticated_s3_get_url), image.authenticated_s3_get_url %></li>
    <% end %>
  </ul>
<% end %>
